# Full example using multiple feature

le name be input("Enter your name: ")
if name is none():
  show("Missing name")
else:
  show("Welcome " + name)

let a be 12
let b be 9
let result be a plus b
show("Result: " + result)

Container as box:
  Text:"Hello, " + name
  Button: "Submit" as primary
